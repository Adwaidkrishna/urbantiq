/**
 * dashboard.js — URBANTIQ Admin Dashboard
 *
 * SPA routing has been removed. Each admin page is now a full
 * standalone HTML file with normal browser navigation.
 *
 * Place dashboard-specific UI logic here (e.g. Chart.js init).
 */
